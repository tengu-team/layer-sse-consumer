name: sse-consumer
summary: SSE consumer
maintainers:
  - Merlijn Sebrechts <merlijn.sebrechts@ugent.be>
description: |
  SSE consumer k8s charm
tags:
  - database
series:
   - kubernetes
resources:
  consumer_image:
    type: oci-image
    auto-fetch: true
    description: "Image used SSE consumer."
requires:
    sse-endpoint:
        interface: sse-endpoint
